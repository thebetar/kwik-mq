services:
    kwik-mq:
        build: .
        ports:
            - '10526:10526'
        deploy:
            restart_policy:
                condition: always
        volumes:
            - ./data:/app/data
        user: '1000:1000'
        stop_grace_period: 30s
        healthcheck:
            test: ['CMD', 'wget', '--spider', '-q', 'http://localhost:10526/health']
            interval: 30s
            timeout: 10s
            retries: 3
